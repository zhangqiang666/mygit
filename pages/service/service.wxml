<!--index.wxml-->
<view class="box"> 
<view class="swiper-tab">
    <view class="swiper-tab-list {{currentTab==0 ? 'on' : ''}}" data-current="0" bindtap="swichNav">
    视频门诊
     <view class="{{currentTab==0 ? 'xiahua' : ''}}"></view>
    </view>
    <view class="swiper-tab-list {{currentTab==1 ? 'on' : ''}}" data-current="1" bindtap="swichNav">
    随身医生
     <view class="{{currentTab==1 ? 'xiahua' : ''}}"></view>
    </view>
    <view class="swiper-tab-list {{currentTab==2 ? 'on' : ''}}" data-current="2" bindtap="swichNav">
    绿色通道
      <view class="{{currentTab==2 ? 'xiahua' : ''}}"></view>
    </view>
</view>
 
<swiper current="{{currentTab}}" class="swiper-box" duration="300" style="height:{{winHeight - 31}}px" bindchange="bindChange">
    <!-- 我是哈哈 -->
    <swiper-item>
      <view class="scroll-box">
      <!--scroll  -->
      <view class="list-car-box" wx:for="{{listdata}}"wx:key="*this">
      <!--  -->
      <view class="list-header">
      <text class="header-left">{{item.numbers}}</text>
      <text class="header-right">{{item.zhuangtai}}</text>
      </view>
      <!--  -->
      <view class="centent-left">
      {{item.nameman}}
      </view>
      <view class="centent-right">
      {{item.name}}
      </view>
      <!--  -->
      <view class="centent-left">
      {{item.phoneman}}
      </view>
      <view class="centent-right">
      {{item.phone}}
      </view>
       <!--  -->
      <view class="centent-left">
      {{item.sexman}}
      </view>
      <view class="centent-right">
      {{item.sex}}
      </view>
       <!--  -->
      <view class="centent-left">
      {{item.problemman}}
      </view>
      <view class="centent-right">
      {{item.problem}}
      </view>
      <!--  -->
      </view>
      <!--scroll  -->
      </view>
    </swiper-item>
    <!-- 我是呵呵 -->
    <swiper-item>
      <view class="scroll-box">
      <!--scroll  -->
      <view class="list-car-box" wx:for="{{listdata}}"wx:key="*this">
      <!--  -->
      <view class="list-header">
      <text class="header-left">{{item.numbers}}</text>
      <text class="header-right">{{item.zhuangtai}}</text>
      </view>
      <!--  -->
      <view class="centent-left">
      {{item.nameman}}
      </view>
      <view class="centent-right">
      {{item.name}}
      </view>
      <!--  -->
      <view class="centent-left">
      {{item.phoneman}}
      </view>
      <view class="centent-right">
      {{item.phone}}
      </view>
       <!--  -->
      <view class="centent-left">
      {{item.sexman}}
      </view>
      <view class="centent-right">
      {{item.sex}}
      </view>
       <!--  -->
      <view class="centent-left">
      {{item.problemman}}
      </view>
      <view class="centent-right">
      {{item.problem}}
      </view>
      <!--  -->
      </view>
      <!--scroll  -->
      </view>
    </swiper-item>
    <!-- 我是嘿嘿 -->
    <swiper-item>
     <view class="scroll-box">
      <!--scroll  -->
      <view class="list-car-box" wx:for="{{listdata}}"wx:key="*this">
      <!--  -->
      <view class="list-header">
      <text class="header-left">{{item.numbers}}</text>
      <text class="header-right">{{item.zhuangtai}}</text>
      </view>
      <!--  -->
      <view class="centent-left">
      {{item.nameman}}
      </view>
      <view class="centent-right">
      {{item.name}}
      </view>
      <!--  -->
      <view class="centent-left">
      {{item.phoneman}}
      </view>
      <view class="centent-right">
      {{item.phone}}
      </view>
       <!--  -->
      <view class="centent-left">
      {{item.sexman}}
      </view>
      <view class="centent-right">
      {{item.sex}}
      </view>
       <!--  -->
      <view class="centent-left">
      {{item.problemman}}
      </view>
      <view class="centent-right">
      {{item.problem}}
      </view>
      <!--  -->
      </view>
      <!--scroll  -->
      </view>
    </swiper-item>
</swiper>
</view>